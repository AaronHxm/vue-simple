import request from '../../../service/request'

/**
 * 保客营销页面
 * @param params
 * @returns {*|Promise|Promise<unknown>}
 */
export const getBkList = {'url':'preleasing/registerCustCtrl/selectRegister',type:'post' }
